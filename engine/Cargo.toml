[package]
name = "stask-engine"
description = "SQLite-backed task tree, custom statuses and undo log behind the stask TUI task manager"
keywords = ["task", "todo", "tree", "sqlite", "undo"]
categories = ["database"]
readme = "../README.md"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
chrono = "0.4.45"
rusqlite = { version = "0.40.2", features = ["bundled"] }
thiserror = "2.0.20"
